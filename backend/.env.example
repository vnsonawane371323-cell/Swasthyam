# MongoDB Connection String
# Replace with your MongoDB Atlas connection string or local MongoDB URL
MONGODB_URI=mongodb+srv://swasthtelcampaign_db_user:1ZgMsyq4erXTouy1@cluster0.fxtgw33.mongodb.net/Swasthyam?retryWrites=true&w=majority

# JWT Secret Key - Use a long, random string in production
JWT_SECRET=daa7718aca76e3fee491fef76c36e846

# JWT Expiration Time
JWT_EXPIRES_IN=7d

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# Frontend URL for CORS (comma-separated for multiple origins)
FRONTEND_URL=http://localhost:8081,http://localhost:19006,exp://192.168.1.100:8081

# AI configuration for medical report analyzer
# Preferred: direct Gemini key
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-1.5-flash

# Optional fallback: OpenRouter key (Gemini model via OpenRouter)
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_OIL_SCAN_API_KEY=your-secondary-openrouter-key
